# From Person B (Project Settings -> API). NEVER commit the real .env.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOi...
# Embedding model — pick once, don't change mid-project (dims must match!)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
